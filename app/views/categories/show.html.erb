<div class="container-fluid category-callout">
  <h1 class="category-name text-center"><%= Category.find(@images.first.category_id).name %></h1>
  <div class="row">
      <% @images.each do |image| %>
        <div class="col-md-4 text-center">
          <div class="artwork-and-text">
            <%= image_tag(image.image.url, class: 'artwork') %>
            <% if image.image_title or image.image_description or image.image_year %>
              <div class="artwork-text d-flex align-items-center justify-content-center">
                <ul class="list-unstyled">
                  <% if image.image_title != "" %>
                    <%= content_tag(:li, image.image_title, class: 'text-center artwork-title') %><hr/>
                  <% end %>
                  <% if image.image_description != "" %>
                    <%= content_tag(:li, image.image_description, class: 'text-center artwork-description') %><hr/>
                  <% end %>
                  <% if image.image_year != "" %>
                    <%= content_tag(:li, image.image_year, class: 'text-center artwork-year') %>
                  <% end %>
                </ul>
              </div>
            <% end %>
          </div>
          <%= image_tag("ducttape-img.png", class: 'top-tape') %>
          <%= image_tag("ducttape-img.png", class: 'bottom-tape') %>
        </div>
      <% end %>
  </div>
</div>